---
version: '3'

networks:
  front-tier:
    external: true

volumes:
  filebrowser:

services:
  filebrowser:
    image: hurlenko/filebrowser:v2.22.4
    container_name: filebrowser
    hostname: filebrowser
    env_file:
      - .env
    expose:
      - 8080
    volumes:
      - /media/filebrowser:/data
      - filebrowser:/config
    restart: unless-stopped
    networks:
      - front-tier
